#!/usr/bin/env bash

set -euo pipefail

repo_url="https://github.com/cyberark/summon.git"

git ls-remote --tags --refs "$repo_url" |
  awk -F/ '{print $NF}' |
  sed 's/^v//' |
  -E '^[0-9]+\.[0-9]+\.[0-9]+$' |
  sort -V |
  xargs echo
